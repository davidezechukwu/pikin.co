// document.addEventListener('DOMContentLoaded', () => {
//     const inputElem = document.querySelector('.pk-number');

//     if (!inputElem) return;

//     inputElem.addEventListener('keydown', handleInput as EventListener);
//     inputElem.addEventListener('keyup', handleInput as EventListener);
// });

// function handleInput(event: KeyboardEvent): void {
//     const target = event.target as HTMLInputElement;

//     if (!target.classList.contains('pk-form')) return;

//     if (target.value.length > 1) {
//         target.value = String.fromCharCode(event.keyCode);
//     }
//   }

//Use for other js stuff